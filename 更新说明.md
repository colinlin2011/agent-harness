# Agent Harness 更新说明

## v1.0.0（2026-02-13）

### 功能概览

长期运行 Agent 通用框架，通过任务配置 + Cursor CLI 实现可复用的多 session 增量完成任务能力。

### 核心能力

1. **Initializer**：首次运行拆解任务为细粒度 work_items，搭建环境
2. **Coding Agent**：每次 session 完成一项，更新 progress 与 work_items，支持跨 session 接力
3. **自动 Git 提交**：Agent 沙箱无法执行 git，脚本在每轮结束后代为 add + commit，确保 git log 可追踪
4. **Cursor Skill**：按行业标准封装，可安装到个人或项目，便于团队复用

### 前置依赖

- Cursor CLI（`agent login` 认证）
- ripgrep（可运行 `install-ripgrep.ps1` 安装）
- PowerShell、Git

### 使用流程

```powershell
# 新建任务
.\agent-harness\scripts\run-initializer.ps1 -ProjectPath .\my-task

# 继续工作（可定时）
.\agent-harness\scripts\run-continue.ps1 -ProjectPath .\my-task
```

### 同步到远程

在 agent-harness 目录：运行 `初始化Git仓库.bat` → `配置远程仓库.bat`（输入仓库地址）→ `同步到远程.bat`
